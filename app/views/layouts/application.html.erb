<!DOCTYPE html>
<html lang="en">
<head>
  <title>CryptocurrencyPortfolio</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>


  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
</head>

<body style="background-color: #EFEFEF">


<div class="d-flex flex-column flex-md-row align-items-center p-3 px-mb-4 mb-3 bg-white border-bottom">
  <h5 class="my-0 mr-md-auto text-uppercase" style="color: #252428; font-family: Impact;">Crypto currency portfolio</h5>
  <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
    <span class="text-uppercase text-dark" title="Go to currencies" style="color: #252428; font-family: Impact;">
      <div class="list-group">
      <a href="/currencies" class="list-group-item list-group-item-action active list-group-item-dark" >Currencies</a>
      </div>
    </span>
  </nav>
</div>


<% unless @user.nil? or @user.id.nil? %>
  <%= link_to "Portfolio", portfolios_path, class: "btn btn-outline-dark btn-sm" %>
  <%= link_to "Profile", users_path, class: "btn btn-outline-dark btn-sm" %>
<% end %>


<div class="container"
<%= yield %>
</div>
<br>

<div style="position:absolute; top:80px; right:20px;">
  <% if logged_in? %>
      <%= button_to "Logout", "/sessions", method: :delete, class:"btn btn-outline-dark" %>
  <% else %>
    <div class="mx-8" style="font-family: Arial; border-color: #666666">
      <form class="button_to" method="get" action="/sessions/new" >
        <input type="submit" value="Login" class="btn btn-outline-dark">
      </form>
    </div>
  <% end %>
</div>

</body>
</html>
