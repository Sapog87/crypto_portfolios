<p style="color: green"><%= notice %></p>

<h1>Deals</h1>

<%= link_to "Back to portfolio", portfolio_path(@portfolio) %>

<div id="deals">
  <% @deals.each do |deal| %>
    <%= render deal %>
  <% end %>
</div>
